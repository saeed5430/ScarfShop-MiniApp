# Plan: Admin Panel Refactoring + Schema Update

## خلاصه تغییرات

۱. حذف جدول `customers` → ادمین فقط کاربران تلگرام (`users`) رو مدیریت کنه
۲. اضافه کردن CRUD ادمین‌ها در پنل
۳. حذف `price` از محصولات (قیمت فقط در سفارشات)
۴. تبدیل `stock` از عدد به boolean در محصولات
۵. اضافه کردن تصاویر چندگانه به محصولات
۶. حذف جدول `variants` → اتصال مستقیم رنگ‌ها و سایزها به محصولات (many-to-many)
۷. اضافه کردن صفحه تنظیمات (اطلاعات تماس + عکس + تنظیمات دیگه)

---

## Phase 1: Database Migration (0013)

### فایل: `worker/migrations/0013_admin_refactor.sql`

```sql
-- 1. حذف customers و اتصال orders به users
DROP TABLE IF EXISTS customers;
ALTER TABLE orders RENAME COLUMN customer_id TO user_id;

-- 2. حذف price از products, تبدیل stock به boolean, اضافه کردن images
ALTER TABLE products DROP COLUMN price;
ALTER TABLE products DROP COLUMN stock;
ALTER TABLE products ADD COLUMN is_stock INTEGER NOT NULL DEFAULT 1; -- boolean
ALTER TABLE products ADD COLUMN images TEXT DEFAULT '[]'; -- JSON array of URLs

-- 3. حذف order_items (وابسته به variants)
DROP TABLE IF EXISTS order_items;

-- 4. حذف variants و junction tables
DROP TABLE IF EXISTS variant_colors;
DROP TABLE IF EXISTS variant_sizes;
DROP TABLE IF EXISTS variants;

-- 5. ایجاد junction tables جدید برای اتصال مستقیم رنگ/سایز به محصول
CREATE TABLE IF NOT EXISTS product_colors (
    product_id INTEGER NOT NULL,
    color_id INTEGER NOT NULL,
    PRIMARY KEY (product_id, color_id),
    FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
    FOREIGN KEY (color_id) REFERENCES colors(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS product_sizes (
    product_id INTEGER NOT NULL,
    size_id INTEGER NOT NULL,
    PRIMARY KEY (product_id, size_id),
    FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
    FOREIGN KEY (size_id) REFERENCES sizes(id) ON DELETE CASCADE
);

-- 6. ایجاد مجدد order_items با ارجاع به product به جای variant
CREATE TABLE IF NOT EXISTS order_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id INTEGER NOT NULL,
    product_id INTEGER NOT NULL,
    color_id INTEGER,
    size_id INTEGER,
    quantity INTEGER NOT NULL DEFAULT 1,
    price INTEGER NOT NULL DEFAULT 0,
    FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES products(id),
    FOREIGN KEY (color_id) REFERENCES colors(id),
    FOREIGN KEY (size_id) REFERENCES sizes(id)
);

CREATE INDEX IF NOT EXISTS idx_order_items_order ON order_items(order_id);

-- 7. ایجاد جدول settings
CREATE TABLE IF NOT EXISTS settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT UNIQUE NOT NULL,
    value TEXT,
    type TEXT DEFAULT 'text' CHECK(type IN ('text', 'image', 'boolean', 'json')),
    label TEXT NOT NULL,
    created_at INTEGER DEFAULT (unixepoch()),
    updated_at INTEGER DEFAULT (unixepoch())
);

-- 8. seed تنظیمات پیش‌فرض
INSERT OR IGNORE INTO settings (key, value, type, label) VALUES
('shop_name', 'فروشگاه آرمانا', 'text', 'نام فروشگاه'),
('phone', '', 'text', 'تلفن'),
('email', '', 'text', 'ایمیل'),
('address', '', 'text', 'آدرس'),
('postal_code', '', 'text', 'کد پستی'),
('telegram_link', '', 'text', 'لینک تلگرام'),
('instagram_link', '', 'text', 'لینک اینستاگرام'),
('logo_url', '', 'image', 'لوگوی فروشگاه'),
('welcome_text', 'به فروشگاه آرمانا خوش آمدید', 'text', 'متن خوش‌آمدگویی'),
('about_us', '', 'text', 'درباره ما');
```

> **نکته مهم:** چون `order_items` به `variants` وابسته بود، اول `order_items` حذف میشه و بعد `variants`. سپس `order_items` جدید با ارجاع به `product_id` + `color_id` + `size_id` ساخته میشه.

---

## Phase 2: Backend API Changes

### فایل: `worker/src/routes/api.ts`

#### تغییرات محصولات:
- حذف `price` از create/update
- تغییر `stock` به `is_stock` (boolean)
- اضافه کردن `images` (JSON array)
- اضافه کردن روت‌های مدیریت `product_colors` و `product_sizes`
- حذف روت‌های variants
- حذف روت‌های variant-relations

#### روت‌های جدید:
```
GET/POST    /api/products/:id/colors    -- لیست/افزودن رنگ‌های محصول
DELETE      /api/products/:id/colors/:colorId -- حذف رنگ از محصول
GET/POST    /api/products/:id/sizes     -- لیست/افزودن سایزهای محصول
DELETE      /api/products/:id/sizes/:sizeId -- حذف سایز از محصول
GET/PUT     /api/settings               -- دریافت/بروزرسانی تنظیمات
GET/POST/PUT/DELETE /api/admins         -- CRUD ادمین‌ها
```

#### تغییرات سفارشات:
- آپدیت `orders` برای استفاده از `user_id` به جای `customer_id`
- آپدیت `order_items` برای استفاده از `product_id` + `color_id` + `size_id` به جای `variant_id`

### فایل‌های Database Class تغییر:

- `worker/src/db/products.ts` — حذف `price`/`stock`، اضافه کردن `is_stock`/`images`
- `worker/src/db/types.ts` — بروزرسانی Product, OrderItem, حذف Variant/Customer
- `worker/src/db/index.ts` — حذف VariantsDB, VariantRelationsDB, CustomersDB
- `worker/src/db/variants.ts` — حذف کل فایل
- `worker/src/db/variant-relations.ts` — حذف کل فایل
- `worker/src/db/customers.ts` — حذف کل فایل
- `worker/src/db/order-items.ts` — بروزرسانی ساختار

---

## Phase 3: Admin Panel Changes

### ۳.۱ حذف مشتریان، اضافه کردن کاربران

**حذف فایل‌ها:**
- `scarf-admin/src/pages/customers/` (کل پوشه)

**ایجاد فایل‌ها:**
- `scarf-admin/src/pages/users/list.tsx` — لیست کاربران تلگرام
- `scarf-admin/src/pages/users/index.tsx` — barrel export

ساختار لیست:
| ستون | توضیح |
|------|-------|
| ID | شناسه |
| نام | first_name + last_name |
| یوزرنیم | username |
| زبان | language_code |
| تاریخ عضویت | created_at (شمسی) |
| آخرین فعالیت | last_active (شمسی) |

> کاربران فقط قابل مشاهده هستند (read-only)، قابل ویرایش/حذف توسط ادمین نیستند.

### ۳.۲ اضافه کردن CRUD ادمین‌ها

**ایجاد فایل‌ها:**
- `scarf-admin/src/pages/admins/list.tsx`
- `scarf-admin/src/pages/admins/create.tsx`
- `scarf-admin/src/pages/admins/edit.tsx`
- `scarf-admin/src/pages/admins/index.tsx`

ساختار فرم:
- نام (name)
- ایمیل (email) — unique
- رمز عبور (password_hash) — فقط هنگام ساخت
- وضعیت (is_active)

### ۳.۳ بروزرسانی صفحه محصولات

**فایل‌های تغییر:**
- `scarf-admin/src/pages/products/create.tsx`
- `scarf-admin/src/pages/products/edit.tsx`
- `scarf-admin/src/pages/products/list.tsx`

#### تغییرات فرم ساخت/ویرایش:

**حذف فیلدها:**
- قیمت (price)
- موجودی عددی (stock)
- SKU (درخواست کاربر: SKU باشه)

**تغییر فیلدها:**
- موجودی → Switch (boolean: موجود/ناموجود)

**اضافه کردن فیلدها:**
- تصاویر (Upload multiple images)
- انتخاب رنگ‌ها (Select multiple + دکمه رنگ جدید)
- انتخاب سایزها (Select multiple + دکمه سایز جدید)

#### ساختار فرم جدید:

```
┌─────────────────────────────────┐
│  اطلاعات پایه                  │
│  ├── نام محصول                  │
│  ├── اسلاگ (خودکار)            │
│  ├── دسته‌بندی (dropdown اسم)   │
│  └── جنس                        │
├─────────────────────────────────┤
│  توضیحات                        │
│  ├── توضیحات کامل               │
│  └── توضیح کوتاه               │
├─────────────────────────────────┤
│  موجودی و SKU                   │
│  ├── موجود (Switch)             │
│  └── کد کالا (SKU)             │
├─────────────────────────────────┤
│  رنگ‌ها                         │
│  ├── Select multiple (با دایره رنگی) │
│  └── + رنگ جدید (Modal)        │
├─────────────────────────────────┤
│  سایزها                        │
│  ├── Select multiple            │
│  └── + سایز جدید (Modal)       │
├─────────────────────────────────┤
│  تصاویر                        │
│  └── Upload multiple (Drag & Drop) │
├─────────────────────────────────┤
│  فعال/غیرفعال (Switch)          │
└─────────────────────────────────┘
```

### ۳.۴ حذف صفحه واریانت‌ها

**حذف فایل‌ها:**
- `scarf-admin/src/pages/variants/` (کل پوشه)

### ۳.۵ اضافه کردن صفحه تنظیمات

**ایجاد فایل‌ها:**
- `scarf-admin/src/pages/settings/index.tsx`
- `scarf-admin/src/pages/settings/settings.tsx`

ساختار صفحه:
```
┌─────────────────────────────────┐
│  اطلاعات فروشگاه               │
│  ├── نام فروشگاه                │
│  ├── لوگو (Upload image)        │
│  └── متن خوش‌آمدگویی            │
├─────────────────────────────────┤
│  اطلاعات تماس                   │
│  ├── تلفن                       │
│  ├── ایمیل                      │
│  ├── آدرس                       │
│  └── کد پستی                    │
├─────────────────────────────────┤
│  شبکه‌های اجتماعی               │
│  ├── لینک تلگرام                │
│  └── لینک اینستاگرام            │
├─────────────────────────────────┤
│  درباره ما                      │
│  └── متن درباره ما              │
└─────────────────────────────────┘
```

### ۳.۶ بروزرسانی main.tsx

**فایل:** `scarf-admin/src/main.tsx`

تغییرات:
- حذف resources: `customers`, `variants`
- اضافه کردن resources: `users`, `admins`, `settings`
- حذف routes مربوطه
- اضافه کردن routes جدید

---

## Phase 4: DataProvider Update

**فایل:** `scarf-admin/src/dataProvider.ts`

تغییرات:
- حذف `customers` از resourceMap
- حذف `variants` از resourceMap
- اضافه کردن `users`, `admins`, `settings` به resourceMap
- اضافه کردن متدهای custom برای product_colors و product_sizes

---

## فایل‌های تحت تأثیر

### Database
- `worker/migrations/0013_admin_refactor.sql` (جدید)
- `worker/src/routes/api.ts` (تغییر)
- `worker/src/db/index.ts` (تغییر)
- `worker/src/db/products.ts` (تغییر)
- `worker/src/db/types.ts` (تغییر)
- `worker/src/db/order-items.ts` (تغییر)
- `worker/src/db/variants.ts` (حذف)
- `worker/src/db/variant-relations.ts` (حذف)
- `worker/src/db/customers.ts` (حذف)

### Admin Panel
- `scarf-admin/src/main.tsx` (تغییر)
- `scarf-admin/src/dataProvider.ts` (تغییر)
- `scarf-admin/src/pages/users/` (جدید)
- `scarf-admin/src/pages/admins/` (جدید)
- `scarf-admin/src/pages/settings/` (جدید)
- `scarf-admin/src/pages/products/create.tsx` (تغییر)
- `scarf-admin/src/pages/products/edit.tsx` (تغییر)
- `scarf-admin/src/pages/products/list.tsx` (تغییر)
- `scarf-admin/src/pages/customers/` (حذف)
- `scarf-admin/src/pages/variants/` (حذف)

---

## Verification

1. اجرای migration جدید روی D1 محلی
2. تست CRUD ادمین‌ها
3. تست نمایش کاربران تلگرام
4. تست ساخت محصول با رنگ/سایز/تصویر
5. تست صفحه تنظیمات
6. بررسی build نهایی (`npm run build` در scarf-admin)
7. بررسی lint (`npm run lint` اگر موجود باشد)
